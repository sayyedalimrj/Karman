# data/incoming/taksa/unknown

Unclassified files land here. Discovery reports them as `UNSUPPORTED` until a
classification rule is added. Raw files are ingestion/analyze inputs only; they
are git-ignored and never read by the website (the UI reads PostgreSQL
metadata). See `../README.md`.
