# data/incoming/taksa/dts

Legacy DTS import packages. Analyzed by `taksa:dts:audit` (safe readable names
only — **never executed**). Raw files are ingestion/analyze inputs only; they
are git-ignored and never read by the website (the UI reads PostgreSQL
metadata). See `../README.md`.
