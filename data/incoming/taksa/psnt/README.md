# data/incoming/taksa/psnt

Taksa PSNT round-trip proposal files. Discovered/registered as metadata; binary
round-trip parsing is deferred to a later phase. Raw files are ingestion/analyze
inputs only; they are git-ignored and never read by the website (the UI reads
PostgreSQL metadata). See `../README.md`.
