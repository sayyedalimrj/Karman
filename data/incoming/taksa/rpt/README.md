# data/incoming/taksa/rpt

Crystal Reports templates. Analyzed by `taksa:rpt:audit` (template metadata +
classification — **Crystal is never executed and no report is generated**). RPT
= templates, not data. Raw files are ingestion/analyze inputs only; they are
git-ignored and never read by the website (the UI reads PostgreSQL metadata).
See `../README.md`.
