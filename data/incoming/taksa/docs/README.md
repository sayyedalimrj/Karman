# data/incoming/taksa/docs

UI/behavior config and tips (`gws.ini`, `TAKSA.xml`, `tx_tips.txt`,
`tx_tips_svz.txt`, `tx_tips_psn.txt`, `read_msp.txt`). Analyzed by
`taksa:docs:audit` for UI labels, DataPropertyName-like keys, and behavior
hints. **Tips are hints, not official rules** until later review. Raw files are
ingestion/analyze inputs only; they are git-ignored and never read by the
website (the UI reads PostgreSQL metadata). See `../README.md`.
