# data/processed/taksa/pdf

PDF metadata audit output (`pdf_summary.json`, `pdf_inventory.csv`) from
`taksa:pdf:audit`. Metadata only — no OCR / no numeric extraction. Generated
artifacts are git-ignored; only this README is tracked.
