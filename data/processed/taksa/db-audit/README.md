# data/processed/taksa/db-audit

DB backup inspection output (`db_backup_inventory.json/.csv`,
`db_restore_status.md`) from `taksa:db:inspect`. Header/metadata only — no
restore. Generated artifacts are git-ignored; only this README is tracked.
