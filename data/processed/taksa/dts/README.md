# data/processed/taksa/dts

DTS audit output (`dts_summary.json`, `dts_inventory.csv`) from
`taksa:dts:audit`. Safe readable names only — never executed. Generated
artifacts are git-ignored; only this README is tracked.
