# data/processed/taksa/rpt

RPT audit output (`rpt_summary.json`, `rpt_inventory.csv`) from
`taksa:rpt:audit`. Template metadata + classification only — Crystal is never
executed. Generated artifacts are git-ignored; only this README is tracked.
