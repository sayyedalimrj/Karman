# data/processed/taksa/backup-strings

Backup-string audit output (`backup_string_summary.json`,
`backup_table_candidates.csv`, `backup_keyword_hits.csv`,
`backup_sensitive_hits_summary.csv`) from `taksa:backup-strings:audit`.
Whitelisted candidates + counts only; sensitive matches are redacted. No
restore, no full dump. Generated artifacts are git-ignored; only this README is
tracked.
