# data/processed/taksa/mdb

MDB audit output (`mdb_summary.json`, and `mdb_tables.csv` / `mdb_columns.csv`
when available) from `taksa:mdb:audit`. Signature + best-effort metadata; no
value import. Generated artifacts are git-ignored; only this README is tracked.
