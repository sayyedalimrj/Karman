# data/processed/taksa/scriptxml

ScriptXML audit output (`scriptxml_summary.json`, `scriptxml_paths.csv`,
`scriptxml_insert_map.csv`, `scriptxml_openxml_map.csv`) from
`taksa:scriptxml:audit`. Static parse only — no SQL executed. Generated
artifacts are git-ignored; only this README is tracked.
