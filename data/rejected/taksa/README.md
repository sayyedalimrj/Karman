# data/rejected/taksa

Holds rejected-input reports produced when a Taksa source cannot be safely
analyzed (unsupported type, unreadable, or failing a safety check). Reports
contain only safe metadata (relative path, reason). Raw rejected files are never
committed; only this README is tracked.
