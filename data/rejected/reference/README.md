# data/rejected/reference

Files that fail strict validation (bad shape, JS-number numeric values, missing
provenance) are recorded here for inspection. Only this README is tracked;
rejected data files are git-ignored and never committed.
